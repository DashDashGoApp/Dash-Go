# Dash-Go screenshot assets

These repository screenshots were captured with Dash-Go Showcase Studio using generic fixture data. Each image is a `2034 × 1144` RGB PNG that has been re-encoded without EXIF, XMP, ICC, PNG text, location, timestamp, author, comment, or software metadata.

The README uses a focused subset. The remaining images are retained for future documentation, release notes, and project pages.
